# 本地 Todo CLI v0.1 开发说明

## 一、需求定义

### 1. 产品定位

实现一个**本地、单用户、可被 Agent 可靠调用的待办命令行工具**。

它是行动项**当前状态的唯一事实源**。OpenClaw、Codex 等 Agent 应通过它查询和更新待办，而不是从 Memory、聊天记录或项目文档中推测哪些事情尚未完成。

核心目标是：**能够一次、完整、确定性地回答“现在还有哪些事情没做完”，且已关闭事项不会因为旧记录或调用重试重新出现。**

工具只负责存储、查询和显式更新，不包含 AI 推断。

### 2. 任务模型与基本操作

父任务和子任务使用同一种任务模型，通过父级关系形成树，不区分成两种实体。每个任务最多有一个父级，支持多层嵌套。

任务包含以下信息：

| 信息       | 要求                                                     |
| ---------- | -------------------------------------------------------- |
| ID         | 稳定、唯一，后续操作通过 ID 定位                         |
| 标题、描述 | 标题必填，描述可选                                       |
| 状态       | `pending`、`in_progress`、`blocked`、`done`、`cancelled` |
| 分类       | `personal` 或 `work`，必须明确                           |
| 项目       | 可选，v0.1 使用项目名即可，不单独实现项目管理            |
| 父任务     | 可选，可以设置、调整或解除                               |
| 原因       | 阻塞原因，以及可选的取消原因                             |
| 来源引用   | 可关联记忆文件、项目文档、外部链接等，不负责读取其内容   |
| 时间       | 创建时间、更新时间、关闭时间，由程序维护                 |
| 创建令牌   | 可选的客户端幂等令牌                                     |

支持创建任务、修改业务字段、清空可选字段、追加进展备注，以及查询详情和历史。

**v0.1 不提供任务硬删除。** 不再需要的任务使用取消操作，保留历史。

### 3. 状态与父子任务规则

**开放状态**为 `pending`、`in_progress`、`blocked`；**关闭状态**为 `done`、`cancelled`。

新任务默认为待处理。支持开始处理、标记阻塞、完成、取消，以及将已关闭任务重新打开为待处理。进入阻塞状态必须提供非空原因；退出阻塞后，旧原因保留在历史中，不再作为当前阻塞原因。关闭时记录关闭时间，重开时清空当前关闭时间，但保留历史关闭记录。

已关闭任务必须通过显式重开才能重新进入开放状态，不能被普通编辑或创建重试隐式重开。

父子关系遵守以下规则：

- 禁止自身作为父级，禁止任何循环关系。
- **完成或取消父任务时，只要仍有开放后代，默认拒绝操作，并返回阻止关闭的任务 ID。**
- 可以显式选择级联完成或级联取消全部开放后代；已经关闭的后代保持原状态，整个操作必须原子完成。
- 子任务全部关闭，不自动关闭父任务；存在已关闭祖先时，不能在其下新增或移入开放任务，也不能重开其后代，必须先重开相应祖先。

重复执行已经生效的同状态操作，不应制造虚假的重复状态变更记录。

### 4. 查询与展示

支持列出任务、按分类／项目／状态组合筛选、按标题和描述进行关键词搜索，以及查看单个任务详情和完整历史。不需要语义搜索。

默认列表只包含开放任务；显式指定关闭状态或选择包含全部任务后，才能查询到已完成和已取消任务。

人类可读输出默认按树展示，子任务不重复列为顶层任务。展示顶层开放任务数、全部开放任务数，以及父任务的子任务进度。进度按**直接子任务**统计，完成数、取消数和总数应能区分，不能把取消当成完成。

**筛选命中子任务时，不能因为父任务未命中而漏掉它。** 为树状展示补充的祖先应标明是上下文，不混入筛选命中数量。

必须支持一次返回全部开放任务，不得隐式截断结果；相同数据和筛选条件下，排序应稳定。

### 5. Agent 接口与幂等

所有核心操作都支持非交互调用，并同时提供人类可读输出和 `--json` 输出。

JSON 的字段、枚举和错误类型需要稳定且有文档。成功和失败都必须可被程序解析；失败使用非零退出码，错误应包含稳定错误码及必要上下文，而不只是自然语言描述。JSON 模式下，标准输出只输出结果 JSON，日志写入标准错误。

创建任务时支持客户端令牌，规则如下：

| 调用情况               | 行为                                 |
| ---------------------- | ------------------------------------ |
| 同一令牌、相同创建内容 | 返回已有任务的当前状态，并标明已去重 |
| 同一令牌、不同创建内容 | 返回冲突，不创建、不覆盖             |
| 不同令牌或未提供令牌   | 视为独立创建，不根据相似标题自动合并 |

**幂等比较针对最初的创建请求，而不是任务后来编辑过的内容。** 任务被修改、完成或取消后，原令牌仍然有效；重试不能创建新任务或改变现有状态。

上述保证必须在并发调用下成立，不能只依赖“先查询、再插入”的非原子判断。v0.1 不要求为所有写操作设计通用幂等协议，但必须完整保证创建幂等。

### 6. 历史与一致性

创建、字段编辑、备注、状态变化、父级变化和提醒关联变化，都必须留下可查询的历史，能够看出**什么时候、发生了什么、哪些内容发生变化**。

任务当前状态直接存储，不要求通过重放历史才能获取，也不需要实现完整事件溯源系统。

业务修改和对应历史必须在同一事务中提交。规则校验失败或操作中断时，不能留下部分修改。虽然是单用户工具，也必须考虑多个 Agent 或 CLI 进程同时访问同一数据库。

### 7. 提醒关联

v0.1 保留提醒关联，但**不调用 OpenClaw API，不负责调度、发送或自动同步提醒**。

一个任务可以关联多个提醒。关联信息包括 OpenClaw 提醒任务 ID、计划时间、时区、投递渠道，以及 `scheduled`／`fired`／`cancelled`／`unknown` 状态。

支持新增、查询、修改关联信息，记录触发或取消，以及解除关联；这些操作都进入任务历史。

一致性与渠道规则：

**外部提醒操作由调用方先完成，再更新本地记录。** 外部删除失败时，调用方不得先移除本地关联。CLI 不把本地记录当成外部状态已经成功改变的证明。

关闭待办不自动删除提醒关联，也不擅自把外部提醒标成已取消。

**工作任务禁止关联微信投递。** 新增关联、修改渠道，以及将任务分类改为工作时，都必须检查这一约束，不能通过编辑绕过。Agent 调用说明还应要求在创建外部提醒前先检查分类，避免先创建微信提醒、再被本地拒绝。

### 8. 与 OpenClaw 的职责边界

| 系统         | 职责                                               |
| ------------ | -------------------------------------------------- |
| Todo CLI     | 行动项当前状态、父子关系、变更历史、提醒关联       |
| Memory       | 个人背景、想法、知识、复盘，不再维护另一份待办状态 |
| 项目追踪文档 | 项目过程、证据、决策和详细进展                     |
| OpenClaw     | 理解自然语言、决定调用什么操作、实际创建和管理提醒 |

“记个待办”“开始做了”“卡住了”“完成了”等自然语言，由 Agent 转换成 CLI 操作。普通想法、地址、菜谱和项目记录不自动变成任务。

首次迁移作为独立使用流程：由 Agent 整理当前仍有效的待办，经人工核对后通过 CLI 写入，保留分类、项目、父子关系和来源。旧记录中已经完成、取消或被新状态覆盖的事项，不得重新导入为开放任务。**本次不开发自动扫描 Memory 或通用迁移系统。**

v0.1 不做云同步、多用户、GUI/TUI、内置提醒调度、日历、复杂标签和优先级、自动推断任务状态，也不直接集成钉钉等其他待办系统。

## 二、技术栈与工程要求

### 1. 技术选型

核心技术栈固定为 **Rust + clap + SQLx + SQLite + Tokio**。

| 用途       | 选型                                                                        |
| ---------- | --------------------------------------------------------------------------- |
| 语言与构建 | Rust stable + Cargo                                                         |
| 命令行解析 | `clap`，采用 derive API。:chatgpt-content-reference{index="0"}              |
| 数据访问   | `sqlx`，直接编写 SQL，不额外引入 ORM。:chatgpt-content-reference{index="1"} |
| 本地数据库 | SQLite                                                                      |
| 异步运行时 | Tokio                                                                       |
| JSON       | `serde` + `serde_json`                                                      |
| 业务错误   | `thiserror`                                                                 |
| 日志       | `tracing` + `tracing-subscriber`                                            |
| ID 与时间  | 优先使用 `uuid`、`chrono`                                                   |

其他辅助依赖自行选择，不需要为了未来可能出现的需求提前引入框架。

### 2. 存储与交付方式

**默认使用用户级的统一数据库，而不是在每个工作目录下创建一份数据库。** 从不同项目目录执行 CLI，应看到同一份待办。允许通过参数或环境变量覆盖数据库路径，便于测试和显式隔离。

首次运行自动创建必要目录、初始化数据库并执行迁移。使用 SQLx migrations，通过 `sqlx::migrate!()` 将迁移嵌入二进制，不要求用户另行部署 SQL 文件；同时确保新增迁移能触发重新构建。:chatgpt-content-reference{index="2"}

优先使用 `query!`／`query_as!` 做编译期检查，并维护离线查询元数据，让构建不依赖用户的实际待办数据库。动态筛选不必为了使用宏而写得过度复杂。:chatgpt-content-reference{index="3"}

SQLite 使用 `SqlitePool`，连接数保持克制；启用外键约束、WAL，并配置合理的锁等待超时。幂等、状态约束和历史一致性仍需通过数据库约束与事务保证。:chatgpt-content-reference{index="4"}

### 3. 实现自由度与验收

项目先保持简单：一个 Cargo package，按命令行、业务逻辑、数据库和输出等职责组织模块即可，不预先拆成多个服务或引入通用框架。

交付内容包括可运行 CLI、数据库迁移、自动化测试，以及面向人和 Agent 的调用说明。文档需说明命令、JSON 成功／失败示例、退出码、数据路径和提醒协作规则。

测试重点覆盖：默认查询不遗漏或混入关闭任务、任务树与统计正确、非法层级和状态操作被拒绝、并发创建幂等、失败回滚、历史完整，以及工作任务的微信渠道限制。

**具体命令命名、表结构、模块划分、JSON 结构和实现算法由 Astra 决定；以上产品语义与一致性约束作为验收依据。**
